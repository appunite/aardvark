[package]
name = "integration-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
assert_cmd = "2.0"
tempfile = "3.10"
serde_json = "1.0"
zip = { version = "0.6", default-features = false, features = ["deflate"] }
